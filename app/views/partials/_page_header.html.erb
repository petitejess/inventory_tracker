<header>
  <!-- Main Navigation -->
  <%= render 'partials/main_nav' %>

  <div class="container">
    <h1>
      <%= content_for?(:html_title) ? yield(:html_title) : "" %>
    </h1>
  </div>
</header>
